{% extends 'base.html.twig' %}

{% block title %}Relais & Châteaux - Accueil{% endblock %}

{% block body %}
<div class="container mt-5">
    <h1 class="mb-4">Bienvenue chez Relais & Châteaux</h1>

    <div class="mb-3">
        {% if app.user %}
            <p>Connecté en tant que <strong>{{ app.user.userIdentifier }}</strong></p>
            <a href="{{ path('app_logout') }}" class="btn btn-danger me-2">Déconnexion</a>
            
            {% if is_granted('ROLE_ADMIN') %}
                <a href="{{ path('admin_dashboard') }}" class="btn btn-primary me-2">Admin Dashboard</a>
            {% endif %}
        {% else %}
            <a href="{{ path('app_login') }}" class="btn btn-success me-2">Connexion</a>
        {% endif %}

        <a href="{{ path('evenement_list') }}" class="btn btn-info">Voir les événements</a>
    </div>

    <hr>

    <h2>À propos</h2>
    <p>
        Découvrez et réservez des salles pour vos événements privés ou professionnels
        dans nos établissements d’exception à travers le monde.
    </p>
</div>
{% endblock %}
